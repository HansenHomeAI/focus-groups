# Focus Groups

A local harness to reduce tunnel-vision in engineering planning.

Given a prompt, Focus Groups:
1) spawns N independent agents (OpenClaw subagents)
2) collects structured findings
3) runs a deliberation round where agents critique each other
4) produces a consensus answer (plus dissent + open questions)

## Status
Early scaffold.
